# Application

So I added an application I already made into the application folder. It was for a similar project. Right now we want to modify it so that it aligns with this particular project. Its nolonger just tobacco based. It allows us to have only one admin. The admin registers a new farmer selects the field and all necessary details about farmer.

The application gets satellite data and then it calculates the score of that farmer. This score should only be based on the agricultural information. The assumption is that there are many other factors that affect thge overal score so we only focus on agriculture. That data will be taken and used  by someone else to do what they do.

I will need a comprehensive short report about that farmer and the data should be stores in the sqlite database.

I understand you might need different machine learning models. Create a plan that we can follow step by step. in CLAUDE.md

